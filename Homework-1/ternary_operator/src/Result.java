public class Result {

    public static boolean guesedRight (int teamOne, int teamTwo, int betTeamOne, int betTeamTwo){


        return (teamOne >= teamTwo) && (betTeamOne >= betTeamTwo) || ((teamOne < teamTwo) && (betTeamOne < betTeamTwo));
        }






        int win = 2;
        int guessedRight = 1;
        int lose = 0;
        int betResultTeamOne;
        int betResultTeamTwo;
    }
    }

